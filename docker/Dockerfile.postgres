FROM postgres:15-alpine

# 设置环境变量
ENV POSTGRES_DB=redink
ENV POSTGRES_USER=redink_user

# 复制初始化脚本
COPY docker/init.sql /docker-entrypoint-initdb.d/

# 暴露端口
EXPOSE 5432

# 数据卷
VOLUME ["/var/lib/postgresql/data"]